name: Dart CI
on:
  push:
    branches:
      - main
  pull_request: {}
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: dart-lang/setup-dart@v1.0
      - name: get
        run: |
          pub get
      - name: test
        run: |
          dart test $(find test -type f) -r expanded
  publish:
    name: publish
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v2
      - uses: dart-lang/setup-dart@v1.0
      - name: get
        run: |
          pub get
      - name: publish
        run: |
          echo "Not implemented as per"
          echo "https://github.com/dart-lang/pub/issues/2227"